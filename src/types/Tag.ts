export interface DifficultyTagProps {
  level: "livre" | "básico" | "intermediário";
}

export interface AvailabilityTagProps {
  status: "encerrado!" | "últimas vagas" | "disponível";
}

export interface QualificationProps {
  level: "estágio" | "trainee" | "júnior" | "pleno";
}
